import React from "react";
// import { Modal } from "reactstrap";

interface Props {
  isOpen: boolean;
}

const BookingModal = ({ isOpen }: Props) => {
  return (
    <>
      {/* <Modal isOpen={isOpen}>
        <label></label>
      </Modal> */}
    </>
  );
};

export default BookingModal;
